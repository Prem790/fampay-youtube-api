# Simple Frontend Dockerfile - Using standard node image
FROM node:18

WORKDIR /app

# Copy package files first for better caching
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy all source files
COPY . .

# Expose Vite's default port
EXPOSE 5173

# Start the development server with --no-open flag to prevent browser auto-open
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173", "--no-open"]